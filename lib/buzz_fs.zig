const std = @import("std");
const api = @import("./buzz_api.zig");
const utils = @import("../src/utils.zig");

export fn mkDir(_: *api.VM) bool {
    unreachable;
}

export fn rm(_: *api.VM) bool {
    unreachable;
}

export fn move(_: *api.VM) bool {
    unreachable;
}

export fn lsDir(_: *api.VM) bool {
    unreachable;
}
