fun main() {
    {str, num} map = {
        "hello": 1,
        "bye": 2,
    };

    | print(map);
    | print(<num, bool>{1: true, 2: false});
    | print(map["hello"]);
    | print(map["bye"]);
    | print((<num, bool>{1: true, 2: false})[2]);
}