object First {
    fun sayHello(str name) {
        print(name);
    }

    fun getHello() > str {
        return hello;
    }
}

fun main() {
    first.sayHello(first.getHello());
}

str hello = "hello world";

First first = First();