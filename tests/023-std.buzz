import "lib/std";

test "parseNumber" {
    assert(parseNumber("12.42") == 12.42, message: "Could parse float");
    assert(parseNumber("not a number") == null, message: "Doesn't parse stupid shit");
}