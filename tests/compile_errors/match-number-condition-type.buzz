// `match` condition must be of type `int`, `double`, `rg` or `type`
import "buzz:std";

test "match number condition type" {
    std\assert(
        match (1) {
            "one" -> "string",
            else -> "number",
        } == "number",
    );
}
