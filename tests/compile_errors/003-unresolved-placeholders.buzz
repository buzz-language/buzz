| `print` is not defined
import "std";
import "tests/compile_errors/utils/bad-import";

test "Unknown string interpolated expression" {
    int hello = 12 + world;

    int by = hello + shey;

    print("{by}");
}

int shey = 42 + joe;