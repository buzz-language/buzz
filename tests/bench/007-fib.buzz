import "std";

fun fibonnacci(n: int) > int {
    if (n < 2) {
        return n;
    }

    return fibonnacci(n - 2) + fibonnacci(n - 1);
}

fun main(_: [str]) > void {
    std\print("{fibonnacci(30)}");
}
