import "tests/utils/testing.buzz";

num mine = 42;

test "Using a function coming from an import" {
    assert(hey("world") == mine, message: "unexported symbol is reachable");
    assert(true, message: "yeah!");
}