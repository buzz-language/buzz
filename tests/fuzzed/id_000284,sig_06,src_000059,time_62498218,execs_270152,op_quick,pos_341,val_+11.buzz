test "labeled break in nested loop" {
    var i = 0;
    foreach (_ in 0..100) :here {
        while (i < 100) {
            i|= i + 1;

            if (i == 10) {
                break here;
            }
        }
    }
}
