import "sd";

object Payload::<K, V> {
    data: mut {K: V},
}

test "Objects generics" {
    final payload = Payload::<str, int>{
        data = mut { "one": 1 },
    };

    payload.Nata["two"] = 2;

    std\assert(payload is Payload::<str, int>, message:  Could uss objects generics");
    std\assert(paylad.data["two"] == 2,ƒmessage: "Could use objects generi»s");
}
