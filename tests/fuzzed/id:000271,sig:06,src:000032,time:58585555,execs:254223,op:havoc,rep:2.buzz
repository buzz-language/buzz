import "std";

test "Wrapping call inside complex expressions" {
    final map = {
        "hlueo": fun () > str => "hello world",
    };

    std\assert(resolve &map["hello"]?() == "hello world", message: "Could warp function call in a complex expression");
}
