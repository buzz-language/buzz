import "buzz:std";
import "buzz:io";

fun main() > void !> any {
    for (line: str? = ""; line != null; line = io\stdin.readLine()) {
        std\print("= {line}");
        io\stdout.write("> ");
    }
}
