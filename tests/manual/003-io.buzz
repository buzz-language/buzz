import "std";
import "io";

fun main(_: [str]) > void !> any {
    for (line: str? = ""; line != null; line = io\stdin.readLine()) {
        std\print("= {line}");
        io\stdout.write("> ");
    }
}
