import "lib/std";
import "lib/buffer";

test "without class" {
    Buffer message = Buffer.init();

    message.write("hello world") catch void;

    str result = message.toString();

    message.deinit();

    print(result);
}