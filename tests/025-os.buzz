import "lib/std.buzz";
import "lib/os.buzz" as os;

test "os.set/env" {
    assert(os.env("HOME") != null, message: "could get env variable");
}

test "os.time" {
    assert(os.time() > 0, message: "Got time");
}

test "os.tmpDir" {
    | TODO: replace by .len check
    assert(os.tmpDir() != "", message: "Got system tmp dir");
}